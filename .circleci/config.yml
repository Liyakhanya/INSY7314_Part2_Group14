version: 2.1

jobs:
  test-backend:
    docker:
      - image: cimg/node:18.17.0
    working_directory: ~/project
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: npm install
      - run:
          name: Fix Mocha permissions
          command: |
            # Make sure mocha binary has execute permissions
            chmod +x node_modules/.bin/mocha
            ls -la node_modules/.bin/mocha
      - run:
          name: Verify Mocha works
          command: npx mocha --version
      - run:
          name: Run security audit
          command: npm run audit
      - run:
          name: Run security tests
          command: npm test
      - run:
          name: Test completion
          command: echo "✅ All 10 security tests completed successfully"

  sonar-scan:
    docker:
      - image: cimg/node:18.17.0
    working_directory: ~/project
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: npm install
      - run:
          name: Install SonarScanner
          command: |
            npm install -g sonarqube-scanner
      - run:
          name: Run SonarQube analysis
          command: |
            sonar-scanner \
              -Dsonar.projectKey=Liyakhanya_INSY7314_Part2_Group14 \
              -Dsonar.organization=liyakhanya \
              -Dsonar.sources=. \
              -Dsonar.tests=tests \
              -Dsonar.test.inclusions=**/*.test.js \
              -Dsonar.host.url=https://sonarcloud.io \
              -Dsonar.token=$SONAR_TOKEN

workflows:
  version: 2
  security-and-quality:
    jobs:
      - test-backend
      - sonar-scan:
          requires:
            - test-backend